var Token1 = artifacts.require("./Token1.sol") 
var Spartan = artifacts.require("./Spartan.sol") 
var Math = artifacts.require("./Math.sol") 
var Pools = artifacts.require("./SPool.sol");

module.exports = async() => {
    // const token1 = await Token1.new();
    // Token1.setAsDeployed(token1)
    // const spartan = await Spartan.new();
    // Spartan.setAsDeployed(Spartan)
    // // const math = await Math.new();
    // // Math.setAsDeployed(math)
    // const pools = await Pools.new(spartan.address);
    // Pools.setAsDeployed(pools)
};